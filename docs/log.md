# Bundle Update Log

## 2026-08-21

* **Update**: RD-03 — at-mark editing and deferred delete. Active annotation cards gain inline note editing and a recolor swatch row (`selection_data.color` override); deletes wait behind a five-second undo window (`use-deferred-delete.ts`, `UndoNotice.tsx`) with unmount-safe commit, visible pending fade on rects/cards, and keyboard-operable Undo. Affected [pdf-reader](/frontend/pdf-reader.md).

* **Update**: RD-02 — explicit highlight lifecycle with recoverable drafts. Highlight and comment creation now flows draft → committing → persisted, or failed with keyboard-operable Retry/Discard in the overlay (`use-highlight-drafts.ts`, `HighlightOverlay.tsx`); all three creation paths share one `persistDraft` in ReaderShell. Added component/hook tests and an e2e spec exercising the fail-then-retry path. Affected [pdf-reader](/frontend/pdf-reader.md).
* **Update**: RD-01 — extracted canonical annotation geometry and an engine-neutral viewer contract. Stored rects are now defined as fractions of the unrotated page (identity at rotation 0, so legacy data needs no migration); `annotation-geometry.ts` gains validation/clamping, displayed↔canonical transforms for all four rotations, and pixel replay across zoom and mixed page sizes (`src/test/annotation-geometry.test.ts`, 16 tests). New `reader/viewer-contract.ts` defines page numbering, metric semantics (unrotated dims at scale 1), scroll-area units, readiness/current-page notification, overlay slot, metrics-aware pointer callbacks, and the imperative handle; the pdf.js engine aliases these types and `ReaderShell` imports only from the contract. Fixed a latent double-scale bug: overlay props previously carried already-rendered dimensions that margin-card math multiplied by zoom again — rendered size is now derived once via `renderedPageSize`. Affected [pdf-reader](/frontend/pdf-reader.md).
* **Update**: RD-00 — self-hosted the pinned pdf.js worker (reader-ai-experience Phase 3). The `copy-pdfjs` postinstall script now also copies `pdf.worker.min.mjs` from the installed `pdfjs-dist` into `public/pdfjs/`; `getPdfWorkerUrl`/`getPdfAssetBaseUrl` in [pdf-viewer](/frontend/pdf-reader.md) point at same-origin `/pdfjs/` paths instead of unpkg, so worker, cmaps, and standard fonts all load locally with a version guaranteed to match the resolved package. VitePWA precaches `pdfjs/*.mjs`. New e2e test asserts zero CDN requests and no missing `/pdfjs/` assets when opening a paper; reader fixtures extracted to `e2e/support/reader-fixtures.ts`. Unit, e2e, and production build all pass.

## 2026-08-16

* **Release decision**: Enabled deep-research starts and resumes after the deterministic lifecycle gate passed. The default `DEEP_RESEARCH_MUTATIONS_ENABLED` setting is now `true`; set it to `false` to stop new starts and resumes immediately. Runs without a configured BYO provider pause safely. Live-provider evaluation remains follow-up work owned by the operator.

* **Update**: Added the opt-in deterministic deep-research lifecycle suite. It runs against local Postgres and proves outbox broker retry, duplicate task delivery suppression, queued cancellation with one terminal event, and expired-worker recovery with stale-lease fencing. Run it with `RUN_DEEP_RESEARCH_INTEGRATION=1 uv run pytest -m integration -q` after loading the local environment. The model-evaluation release gate remains blocked until a provider is configured.

* **Update**: Hardened deep-research lifecycle ownership after review. Starts support a per-owner `Idempotency-Key`; queued cancellation now has a terminal cancellation path; terminal session state and its SSE event commit together; report URLs no longer create evidence; owner metadata matches the non-null cascade migration; and generation lease tokens fence stale workers after recovery. The mutation freeze remains while the broker/worker-kill integration and real eval release gates are unfinished.

* **Update**: Added the next deep-research lifecycle foundation: transactional generation outbox dispatch, generation and outbox leases, duplicate-delivery suppression, periodic abandoned-lease recovery, cooperative cancellation, and an active-delete cancellation policy. Added a normalized generation evidence ledger, report-link verification, configurable turn/evidence bounds, a versioned eval-contract fixture, and raw reasoning redaction. Mutations remain frozen while integration and release-eval gates are incomplete. Updated [the reformation plan](/features/reader-ai-experience.md), [the API](/backend/api/deep-research.md), [tasks](/backend/tasks.md), and [models](/backend/models.md).

* **Update**: Added the DR-11 durable event-store and cursor SSE checkpoint: generation-scoped Postgres events, signed opaque cursors, stale-generation rejection, durable retrying events, cursor-aware frontend reconnects, fatal HTTP handling, bounded backoff, and SSE parser ID support. Added backend and frontend event/cursor tests.

* **Update**: Added the DR-10 durable deep-research state foundation: non-null owner migration with orphan quarantine, checked session versions and transitions, generation/event/evidence/message/outbox models, bounded payload helpers, and generation records on start/resume. Exercised migration upgrade and downgrade against local Postgres.

* **Update**: Completed the DR-03 worker-capacity checkpoint: interactive Celery workers now exclude `research`, dedicated research workers consume only `research` at concurrency 1, health reports interactive and research worker roles separately, and Redis-only deployments no longer claim AMQP broker dead-letter support. Updated queue and Docker documentation.

* **Update**: Started DR-02 input hardening with bounded research questions, transactional active-run limits, bounded research payloads, SSRF-safe pinned downloads, redirect and response-size limits, PDF validation, upload/text batch limits, and authenticated discovery add-to-library ingestion. Updated the [reader and AI experience plan](/features/reader-ai-experience.md).

* **Update**: Added the DR-01 tenant-scope boundary for AI paper tools, including fail-closed identity handling, owner/share/group authorization coverage, vector search filtering, cited-paper containment, and admin capability propagation. Updated the [reader and AI experience plan](/features/reader-ai-experience.md).

* **Planning**: Added the [Reader and AI Experience Reformation](/features/reader-ai-experience.md), a gated implementation blueprint covering the PDF reader, shared AI/chat presentation, Thinking Orbs, license-gated duotone icons, illustrated empty states, and a focused conversational deep-research rewrite. The plan records exact file surfaces, dependencies, checkpoints, acceptance criteria, security gates, tests, evals, rollout, and rollback. Updated the [features index](/features/index.md).
* **Update**: Began Phase 0 with a universal safety freeze for legacy deep-research starts/resumes (`DEEP_RESEARCH_MUTATIONS_ENABLED=false` by default), added a matching frontend gate, and documented the local hot-reload topology with separate API, interactive-worker, research-worker, beat, and Vite terminals. Local Postgres/pgvector and Redis services are running for development.
* **Update**: Applied the provided development configuration to ignored local env files only: local Postgres (`localhost:5433`, database `papers`), Redis, localhost frontend/API URLs, supplied discovery/embedding credentials, local admin login, and email disabled. Production domains, TLS, and production email configuration were omitted.
* **Update**: Added the loopback frontend origins to the debug CORS allow-list so local OAuth/API requests work from either `localhost:5173` or `127.0.0.1:5173`.
* **Update**: Repaired the backend test baseline: plain `uv run pytest` now imports `app` and runs async tests through project configuration; stale paper and Agents SDK tool tests now match current contracts. Result: 126 passed, 1 skipped.
* **Update**: Added the frontend verification skeleton: Vitest/jsdom component tests, a Playwright Chromium login smoke test, Bun test scripts, and documented frontend test commands. Component test, browser smoke, and production build all pass.
* **Update**: Expanded the browser fixture through authenticated reader → Chat-panel flow with a deterministic PDF/API fixture. The test exposed and fixed a React Strict Mode tab-registration race by making paper tab IDs stable and idempotent. Component tests, both browser smokes, and the production build pass.
* **Update**: Expanded QA review surfaces with deterministic dark/light, reduced/full motion, compact/comfortable density, narrow/wide, long-copy, offline, error, loading, and AI-activity fixtures. Added the standards audit for barrel-only icons and explicit duotone metadata; all four Playwright review/browser tests pass.

## 2026-07-15

* **Feature (implemented)**: **Deep Research** shipped. Backend — new `DeepResearchSession` model / `deep_research_sessions` table (migration `deep_research_001`, `down_revision = "add_last_opened_at"`, now the Alembic head), `schemas/deep_research.py`, `services/deep_research_service.py` (drives `create_deep_research_agent` with a generous single-pass turn budget; **two persistence layers** — a replayable Redis event relay `deepresearch:{id}:events` and a `to_input_list()` `run_state` checkpoint — with failure routed by the `agent/error.py` taxonomy: recoverable → `DeepResearchRetryable`/retry-from-checkpoint, auth/no_provider → `paused`, else `failed`; **no throttling**), `tasks/deep_research_tasks.py` (`run_deep_research_task`, Celery name `research.run_deep_research`), a dedicated Celery **`research`** queue (own exchange + DLQ) in `celery_app.py`, and router `api/deep_research.py` at `/api/v1/deep-research` (start/list/detail/delete/resume + resumable SSE stream). Frontend — `deepResearchApi`, `chatStreamClient.streamDeepResearch` + `streamingFetchGet`, `useDeepResearchStream`, a chat-style thread (`ReasoningTree`, `ResearchComposer`) at `/deep-research` and `/deep-research-archive`, using the in-house `components/icons/` set. Affected [deep-research feature](/features/deep-research.md), [deep-research API](/backend/api/deep-research.md), [models](/backend/models.md), [tasks](/backend/tasks.md), [database](/backend/database.md), [api index](/backend/api/index.md), [chat-system](/frontend/chat-system.md), [hooks](/frontend/hooks.md), [routing](/frontend/routing.md), [components](/frontend/components.md), [architecture](/architecture.md), [backend index](/backend/index.md). ADRs: [resumable long-running agent runs](/decisions/resumable-agent-runs.md), [dedicated `research` queue / always-on](/decisions/deep-research-queue.md).
* **Update**: Deep research is **always-on with zero env vars** — the last flag, `ENABLE_DEEP_RESEARCH`, was removed from `config.py`, root `.env.example`, and both compose files; the worker `-Q` list gains `research`. Corrected the stale Alembic-head references (`citation_map_001` → `deep_research_001`) across the bundle. Affected [config](/backend/config.md), [env-config](/infra/env-config.md), [docker](/infra/docker.md), [database](/backend/database.md), [architecture](/architecture.md), [backend index](/backend/index.md), [deep-research feature](/features/deep-research.md).

## 2026-07-12

* **Update**: Redesign branch closing pass — [Settings](/frontend/components.md) split from an 828-line monolith into a 29-line shell + `pages/settings/` sections (anchor nav, token theme swatches); Dashboard rebuilt on one DashCard shell with a tokenized Recharts pass (chart-* series, popover tooltips, donut with total+legend, uniform stat tiles); mechanical sweep finished (all `!` size overrides and `rounded-md` gone, non-token page widths on width tokens, Discovery's inline toggle → segmented Tabs). Live-testing fixes: discovery relevance score normalized (was "8412% relevant"), panel-tab switching fallback, count badges unclipped, "Show less" unflipped, Highlights tab → Details auto-highlight button, composer mention overlay alignment. Audit: 224 → 9 (all remaining are the quarantined shadcn island or false positives). Build green; lint errors below the pre-redesign baseline.

* **Update**: Redesign branch continued — full icon migration landed (91 files; iconsax/hugeicons/lucide uninstalled with an eslint import guard; semantic fixes: state-aware panel toggles, real maximize/minimize, one CheckIcon). Loading/empty/error states unified app-wide on `Spinner`/`Skeleton`/`EmptyState`/`ErrorState` (query-errors-inline, mutation-errors-toast; raw status palette at zero outside the shadcn island). Mobile passes: [PapersList redesigned mobile-first](/frontend/components.md) (PageContainer, segmented toolbar, responsive grid, table→cards below md) and the Finder toolbar wraps at phone widths with 40px touch targets. New Button standard: icon+label never render together (label ≥sm, icon-only below). UX fixes from live testing: panel-tab switching fallback, un-clipped count badges, un-flipped "Show less", Highlights tab folded into a Details-header auto-highlight button (`AutoHighlights.tsx` deleted), composer mention overlay de-iconed. HeroUI field outlines restored (`--field-border-width: 1px`), dropdown shadows flattened. Affected [components](/frontend/components.md), [styling](/frontend/styling.md), [icon-system](/frontend/icon-system.md).

* **Creation**: Frontend redesign underway on branch `redesign/frontend-standards`. New [icon system](/frontend/icon-system.md) — ~109 hand-drawn glyphs (rounded outline, 1.5u/24-grid, `createIcon` factory, filled status variants, dev sheet at `/dev/icons`) replacing iconsax/hugeicons/lucide. New design tokens in `index.css`: status intents (`--danger/--warning/--success/--info` + color-mix soft/border surfaces), `--chart-1..4`, `--overlay-scrim`, `--header-h`, `--width-reading`, real `--font-mono`, global `prefers-reduced-motion` (spinner-exempt); headings now reference the `@theme` scale. New standards primitives: `ui/Spinner`, Button `loading` + icon sizes, `SkeletonText`, `ui/EmptyState`, `ui/ErrorState`, `layout/PageContainer`, Tabs `segmented` variant; `customToast` deleted (sonner intents only); `ai/ErrorBanner` tokenized. Affected [styling](/frontend/styling.md), [components](/frontend/components.md).
* **Update**: **HeroUI v3 adopted as the primitive layer** (user decision mid-redesign): `@heroui/react` 3.2.2 + `@heroui/styles`, themed via a CSS-var bridge in `index.css` mapping HeroUI's contract vars onto Lumen primitives (`--accent` remapped from mint to the forest-black action color; button pill radius overridden to `--radius-interactive`; `data-theme` stamped alongside `.dark`). `ui/*` facades keep their historical APIs while rendering HeroUI/React Aria internals: Button, Input, Textarea, Select, Dialog (Modal), Badge (Chip), Progress, SearchInput (InputGroup), Skeleton (shimmer). Tooltip/Popover/Accordion/Pagination deliberately remain in-house for now. QA surface: dev route `/dev/ui`. Affected [components](/frontend/components.md), [styling](/frontend/styling.md).

* **Update**: Local-run env loading corrected across [setup](/infra/setup.md), [env-config](/infra/env-config.md), and `README.md`: pydantic-settings resolves `env_file=".env"` against the process cwd, so a backend run from `backend/` never reads the repo-root `.env` — the documented approach is now `set -a; source ../.env; set +a` plus overrides (`DB_HOST=localhost`, `DB_PORT=5433`, `REDIS_HOST=localhost`, `STORAGE_PATH=./storage/papers`, local URLs, `DEBUG=true`) in every backend terminal. Also recorded: compose Redis has no `--requirepass` so `REDIS_PASSWORD` must stay empty; API + Celery must share `JWT_SECRET_KEY`/`AI_KEY_ENCRYPTION_KEY` (worker decrypts what the API encrypted — per-terminal random keys break it). README's Docker-dev section now uses `testing.yourdomain.com` placeholders with a pointer to the hardcoded `Host()` labels in `docker-compose.dev.yml`.
* **Update**: Aligned `README.md` with the documented app state: AI sections rewritten for the [BYO AI providers](/decisions/byo-ai-providers.md) model (server `GOOGLE_API_KEY` = embeddings only; per-user Gemini/OpenAI/Anthropic/DeepSeek/OpenAI-compatible keys, Fernet-encrypted) and the [agents-SDK chat path](/backend/services/ai-agent.md) incl. multi-paper/group chat; discovery features expanded (arXiv/Semantic Scholar/OpenAlex/Google Scholar, AI search stream, recommendations, HuggingFace Daily Papers) per [discovery API](/backend/api/discovery.md); architecture bullets refreshed (21 routers, Scalar docs, queue/DLQ/beat layout, React Router v7, Tailwind v4, PWA, citation-map viz, `landing/` site, Lumen naming); removed stale claims (`gemini-3-flash-preview` generation model, TipTap editor — no longer a dependency); added OpenAlex + Resend to external dependencies.
* **Creation**: Added [setup guide](/infra/setup.md) (type: Guide) covering the three run modes — local instance (no Docker), Docker dev (`docker-compose.dev.yml`), Docker prod (`docker-compose.prod.yml`) — and listed it in [infra/index](/infra/index.md). Records that both Docker paths run migrations automatically (backend image CMD), dev hardcodes `*.testing.maurc.org` hosts (needs `/etc/hosts`), dev Postgres maps to host port 5433, and prod bind-mounts `./backend/storage` + derives `FRONTEND_URL`/`VITE_API_URL` from the domain vars.
* **Update**: Rewrote the repo `README.md` "Getting Started"/"Production Deployment" sections to match reality: replaced the broken `docker compose up -d` quick start (no `docker-compose.yml` exists) with a three-mode table + per-mode instructions; added the `/etc/hosts` + dev-URL env setup for Docker dev; added missing `AI_KEY_ENCRYPTION_KEY` and `VITE_GOOGLE_CLIENT_ID` to env examples; fixed the Celery queue list (added `discovery`) and `DB_PORT=5433` for the local path; fixed prod `mkdir -p data/storage` → `backend/storage`; removed the manual `alembic upgrade head` exec steps (migrations are automatic). Affected [setup](/infra/setup.md), [docker](/infra/docker.md), [env-config](/infra/env-config.md).
## 2026-07-11

* **Update**: `AiReading` panel screenshots now fill their gradient panel with `size-full object-cover object-top` (was natural-height `h-auto w-full`), fixing large empty space below ultra-wide shots (e.g. `discovery-1.png`) inside the fixed-min-height bounce cards on mobile. Affected [landing app](/landing/app.md).
* **Update**: Landing screenshot sizing pass. (1) `Features` cards now render shots in a fixed-height frame (`h-[280px]`/`lg:h-[320px]`, `object-cover object-left-top`) bleeding into the card's bottom-right corner via negative margins, with a flex spacer pinning the shot to the card bottom — cards in a grid row are now equal-height regardless of each screenshot's aspect ratio. (2) `AiReading` grid swap: "Chat with papers" moved to the wide `md:col-span-8` card using landscape `chat-alongside.png`; portrait `summary.png` (474px wide — was being upscaled ~1.5× and heavily cropped in the wide card) moved to a narrow `md:col-span-4` card; `chat-tab.png` is no longer referenced. (3) `HowItWorks` mockups are height-capped on desktop via new `IMG_CROP`/`IMG_CROP_RIGHT` constants in `landing/src/lib/img.ts` (`lg:h-[520px] object-cover`, crop anchored to the corner nearest the copy); `Carousel` gained an `imgClassName` prop to pass these through to its front image — fixes the near-square `discovery-3.png` towering over its copy and the step-2 carousel changing height per slide. Affected [landing app](/landing/app.md).
* **Update**: Landing-page design pass (motion, a11y, copy). (1) Reduced-motion support: `App.tsx` wraps the page in `MotionConfig reducedMotion="user"`; marquee animation moved from inline style to `.marquee-row`/`.marquee-row-reverse` classes in `index.css` with a `prefers-reduced-motion` override that also disables smooth scroll. (2) Motion timing tightened: `about-section-2` stagger 0.4s→0.08s per item, `section-with-mockup` stagger 0.2s→0.08s and travel 50px→20px, durations ~0.45-0.5s. (3) All CTAs got `active:scale-[0.97]` press feedback with explicit `transition-[background-color,transform]` (Hero, Navbar, Providers, FinalCta, mobile menu). (4) `Carousel` is now a real `<button>` with a `focus-visible` outline; fixed dead `bg-forest-black/35` class → `bg-forest/35` on the progress-dot pill. (5) A11y/semantics: `Features` cards split body copy out of the `<h3>` into a `<p>`; Navbar hamburger got `aria-expanded`; mobile menu gained the missing Sign-in link; `text-white/55` bumped to `/70` on dark sections and `text-cool-gray` → `text-mid-gray` in `AiReading` for contrast. (6) Copy: FinalCta headline rewritten to "Your library. Your keys. Your infrastructure." with matching body/CTA ("Get started free"); curly apostrophe in the Features heading. (7) Fonts: dropped unused Inter 900 from the Google Fonts request; fixed `--font-mono` (listed Inter first, so mono never rendered monospace); hero screenshot gets `fetchPriority="high"`. Affected [landing app](/landing/app.md).

## 2026-07-04

* **Update**: Reader/chat UX pass. (1) `PaperCard` now previews `Paper.ai_summary` (already returned by the backend `Paper` schema) instead of raw `content_text` scraped by pymupdf; renders nothing until a summary exists. (2) Zen/fullscreen reading (`ReaderShell`'s `zen` state) now also collapses the PDF viewer's thumbnail/outline sidebar and suppresses the annotation margin-card layout via new `PDFViewerHandle.setThumbnailSidebarOpen`/`getThumbnailSidebarOpen` methods, so the page recenters using the existing `flex justify-center` page layout — restored on exit. (3) The right-side panel tab (Details/Insights/Chat/...) is now stored per paper tab as `Tab.panelTab` in [state-management](/frontend/state-management.md)'s `TabContext`, so switching between open paper tabs no longer resets it to "Details"; `Layout.tsx` derives `activeTab` from the active tab instead of local state. (4) Replaced several full-page/panel loading spinners with `Skeleton`-based placeholders (`PaperDetail`, `ReaderShell`'s PDF-load state, `RelatedPapers`, `DiscoveryArchive`, and the initial-fetch states of `AISummary`/`KeyFindings`/`ReadingGuide`) — inline button-pending spinners (regenerate/export/etc.) were left as-is. (5) Restyled the chat tool-call list and reasoning panel (`ToolCallIndicator`, `AgentThoughtPanel`) into a single bordered/muted card with status icons instead of a bare list of colored dots. (6) Fixed a post-stream chat flicker: `useChatController`'s delayed cache invalidation after a turn completes now uses `refetchType: 'none'` on the visible session query — see [chat-system](/frontend/chat-system.md) — so it no longer force-refetches and swaps optimistic (temp-id) messages for server ones (which was remounting the message list) right after streaming finishes.

## 2026-07-03

* **Reformation batch 5 (frontend UX)**: (1) Softer 401 handling — `fetchApi` now classifies refresh failures as `auth_failed` (401/403 → session teardown) vs `transient` (network/5xx → 1s backoff + one retry, session preserved); concurrent 401s share one refresh promise; `AuthContext.scheduleRefresh` retries transient failures 3× with backoff before `clearAuth()`. (2) Chat rate-limit retry UX — `useChatStream` auto-retries `rate_limit` errors (2 attempts, 5s/15s backoff), exposes `autoRetryAt`; `ErrorBanner` shows a live countdown; controller toasts "retrying in Ns" instead of an error. (3) Semantic-search-unavailable signal — `EmbeddingService.is_available()`, `GET /search/capabilities`, `semantic_available` on `SearchResponse` (POST `/search` short-circuits to empty results when no embedding key), amber notice + "Use full-text" fallback on the Search page. (4) Server-side recents — `user_paper_state.last_opened_at` (migration `add_last_opened_at`), touched in `increment_view_count`; `GET /papers/recent`; Dashboard prefers "Recently Opened" over "Recently Added" once present. Affected [api-layer](/frontend/api-layer.md), [chat-system](/frontend/chat-system.md), [infra](/backend/api/infra.md), [papers](/backend/api/papers.md), [database](/backend/database.md). Batch 4 (route/DB integration tests) skipped per owner decision — no testing setup wanted for now.

## 2026-07-02

* **Update**: Toast system overhaul — moved to top-right, no close buttons, compact sizing, spring animations, click-to-dismiss on error toasts. Processing errors auto-toast with module-level dedup (survives mount/unmount). Chat stream errors now map to specific user-friendly messages (invalid key, insufficient balance, rate limit, etc.) via `toastChatError()`. Affected [state-management](/frontend/state-management.md), [toast helpers](/frontend/api-layer.md), [components](/frontend/components.md).
* **Review fixes**: Batch 2/3 review of the uncommitted per-user rate-limit + provider-timeout work. Added Alembic migration `add_provider_timeout_seconds` (server_default 120, backfills existing rows); guarded `timeout_seconds` against NULL/missing attribute in `helpers.py` and `provider_factory.py` (UserAISettings has no such column); rate limiter no longer counts rejected attempts against the window and uses uuid members; removed the DLQ auto-retry beat task (`retry_dead_letter_queue`) — the Redis broker ignores `x-dead-letter-*` queue arguments so the DLQ can never receive messages; fixed pre-existing `selectinload` NameErrors in two `paper_tools.py` agent tools. Affected [tasks](/backend/tasks.md), [ai-providers](/backend/services/ai-providers.md), [database](/backend/database.md).
* **Update**: Consolidated CRUD — moved `user_ai_provider.py` and `user_ai_settings.py` from top-level `crud/` into `api/crud/`, updated all imports, deleted `backend/app/crud/`. Affected [crud](/backend/crud.md), [backend index](/backend/index.md). ADR at [consolidated-crud](/decisions/consolidated-crud.md).
* **Update**: Sunset legacy AI abstractions — deleted `BaseAIService` (shim) and `BaseGoogleAIService` (deprecated shim); removed `AIProviderRegistry` singleton (replaced with flat dict in `provider_factory.py`); inlined `_get_provider`/`_build_config` into `ChatService` and `AISearchService`; `MultiChatService` no longer inherits from anything. Affected [ai-providers](/backend/services/ai-providers.md), [architecture](/architecture.md). ADR at [sunset-legacy-ai-provider-path](/decisions/sunset-legacy-ai-provider-path.md).

## 2026-07-01

* **Update**: Added repo-root `LICENSE` — PolyForm Noncommercial 1.0.0, owner blackprince001 (reformation "additional" §4). Also removed `DEEP_RESEARCH_DAILY_CAP` from `Settings`, `.env.example`, and both compose files — the revised deep-research approach won't use a daily cap; only `ENABLE_DEEP_RESEARCH` remains. Affected [config](/backend/config.md), [env-config](/infra/env-config.md), [deep-research](/features/deep-research.md).
* **Reformation batch 1**: `init_db()` no longer runs `create_all` outside DEBUG ([ADR](/decisions/migrations-own-schema.md)); global HTTP error envelope `{code, message, detail}` via `app/core/error_handlers.py` + `ApiError.code` on the frontend ([ADR](/decisions/http-error-envelope.md)); new `AI_KEY_ENCRYPTION_KEY` with MultiFernet legacy fallback ([ADR](/decisions/separate-encryption-key.md)). Affected [database](/backend/database.md), [security](/backend/security.md), [config](/backend/config.md), [entry-point](/backend/entry-point.md), [api-layer](/frontend/api-layer.md).
* **Update**: Removed `DEEP_RESEARCH_MODEL`/`_MCP_URL`/`_MCP_TOKEN` from `.env.example` and both compose files — the revised [deep-research](/features/deep-research.md) approach won't need an external MCP config; its plan is marked for architectural revision. JWT hard-fail-in-prod was considered and declined; warn-and-generate stays.

## 2026-06-28

* **Creation**: Added the [reformation assessment](/reformation.md) (type: Assessment) — a whole-project review of usage-side pitfalls, backend improvements, feature considerations, and additional things to track; references the [deep-research](/features/deep-research.md) feature plan.
* **Creation**: Added the [deep-research feature plan](/features/deep-research.md) (type: Feature Plan) as the first entry in [/features/](/features/index.md); referenced the existing-but-unmodeled `DEEP_RESEARCH_*` env vars (drift noted in reformation §7).
* **Update**: `.gitignore` blanket-ignored `*.md` repo-wide, which silently defeated the OKF bundle — added `!docs/`, `!docs/**`, `!CLAUDE.md` negations so the bundle and `CLAUDE.md` are version-controlled. Tracked in reformation §7 item 1.
* **Update**: Removed `ai-lifecycle.md` (AI-lifecycle concept no longer needed) and added a [features/](/features/index.md) directory, which serves as the home for forward-looking feature-planning concepts. Listed [features](/features/index.md) and [reformation](/reformation.md) in the root [index](/index.md).
* **Initialization**: Generated the OKF v0.1 knowledge bundle from a full codebase scan. Created the bundle root [index](/index.md), the [architecture overview](/architecture.md), and concept trees under [backend](/backend/), [frontend](/frontend/), [landing](/landing/), [infra](/infra/), and [decisions](/decisions/).
